# DocSuree Git 규칙

## 브랜치
- `main` — 항상 동작하는 상태 유지. 직접 커밋 금지(문서 수정은 예외).
- 작업 브랜치: `feat/<주제>`, `fix/<주제>`, `docs/<주제>` (예: `feat/pdf-parser`)
- 작업 완료 후 main에 merge하고 브랜치 삭제.

## 커밋 메시지
`<타입>: <요약>` 형식, 요약은 한국어로 간결하게.

| 타입 | 용도 |
|------|------|
| feat | 기능 추가 |
| fix | 버그 수정 |
| docs | 문서 (기획서, README 등) |
| refactor | 동작 변화 없는 코드 정리 |
| test | 테스트 추가·수정 |
| chore | 설정, 빌드, 기타 잡일 |

예: `feat: PDF 텍스트 추출 파이프라인 추가`

## 커밋 단위
- 한 작업 = 한 커밋. 다듬는 중간마다 올리지 말고 끝나고 한 번 올린다.
- 계획 문서의 task 는 진행 추적 단위지 커밋 단위가 아니다.
- 제목은 포괄적으로, 세부는 본문에 항목으로.

## 푸시
- 원격: `origin` = 사내 Gitea 의 팀 저장소 (주소는 `git remote -v` 로 확인)
- 하루 작업 끝나면 반드시 push.

## 금지
- `.env`, 인증키, 사내 데이터 원본 커밋 금지 (.gitignore 확인)
- 50MB 넘는 파일 커밋 금지 — 대용량 산출물은 공유 드라이브 사용
- claude 모든 로그 커밋 금지 - 대용량 커밋은 한번만 

