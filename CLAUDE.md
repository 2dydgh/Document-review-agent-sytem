# DocSuree 개발 규칙

이 저장소의 모든 세션에 가장 최상단에서 적용되는 공용 규칙. 반드시 준수. 

## 프로젝트
- DocSuree: 문서 검증 AI 에이전트 플랫폼 (PoC). PDF·Word·HWP 업로드 → 규칙+LLM 검사 → 지적 위치 하이라이트 리포트.
- 금년 범위: Lv1(문서 내 검토 13개) + Lv2(체크리스트 검증 5개) = 18개 항목, 9/21 1차 배포에 전부 포함. 범위 임의 변경 금지.
- 파서: 8/1까지 PDF·Word·HWP 셋 다 파싱 동작 → 이후 정확도 점진 개선 → 완료시 해당 정의 삭제

## 성능 스펙 (설계 기준)
- 동시 접속 10명 이상, 최대 사용자 50명. 이 스펙을 넘는 확장 설계 금지.
- 문서 1건 검사 최대 5분 이내 목표. 검사 요청은 작업 큐로 처리, 웹 응답 블로킹 금지.
- 전 과정 사내망 처리.

## 아키텍처 — 모듈식 (최우선 원칙)
- 모든 기능은 `src/modules/<이름>/` 독립 모듈로: README(입출력 스키마·사용법) + 공개 인터페이스만
  export하는 `__init__.py` + `tests/`. 다른 프로젝트에 폴더째 복사해도 동작해야 한다.
- 모듈 간 연결은 공개 인터페이스로만, 다른 모듈 내부 구현 import 금지.
- DocSuree 조립(API·화면·계정)은 `src/app/`에만 — 모듈은 DocSuree를 몰라야 함.
- 루트 구조: 프론트 `web/` · 팀별 기준 데이터 `presets/` · 정답 재검증 셋 `golden/`.
- 초기 모듈 10개: `doc_parser` `llm_client` `preset` `report` +
  Agent 6종 `agent_format`(형식·완전성) `agent_quality`(표현·내용 품질) `agent_trace`(정합성·추적성)
  `agent_checklist`(표준·체크리스트) `agent_generate`(작성·생성 지원) `agent_history`(의견·이력 관리).
- 규칙 기반 검사와 LLM 판정은 분리 — 규칙 검사는 LLM 없이 동작해야 한다. LLM 호출은 `llm_client` 단일 창구(모델 교체 가능하게 설계 필수).
- **환각은 코드로 막는다 — 모델에게 묻지 않는다.** 이 제품의 핵심 계약이다.
  모델에게 confidence 를 묻거나 한 번 더 물어 자기검증시키지 않는다. 대신:
  1. 모델은 지적과 함께 **원문을 그대로 인용**해야 한다.
  2. 그 인용이 문서에 실재하는지 **문자열로 대조**한다 — 공백만 정규화하고 글자는
     건드리지 않는다. 글자를 바꾸는 것은 환각이다.
  3. 통과한 근거가 하나도 없으면 **그 지적은 폐기**한다.
  4. 버린 건수는 **INFO 로 드러낸다.** 조용히 지우면 "지적이 없다"는 거짓말이 된다.
  짧은 인용(`"예"` · `"|"`)은 아무 문서에나 있어 지어낸 지적을 통과시키므로 최소 길이와
  문자/숫자 포함을 요구한다. 단 이 문턱은 인용 하나가 아니라 **지적 전체**에 건다 —
  짧은 인용 하나가 이미 검증된 진짜 근거까지 끌고 죽지 않게.
- **모르면 모른다고 말한다.** "0건 통과"와 "검토를 못 했다"를 절대 섞지 않는다.
  요건 ID 를 하나도 못 찾으면 · LLM 호출이 실패하면 · 후보가 상한에 걸려 잘리면,
  전부 미검토·절단 사실을 드러낸다. 지적 0건으로 보이게 두지 않는다.
- 도메인 데이터(팀별 규칙·체크리스트·용어집)는 코드와 분리 — 엔진에 팀명·문서명·규칙값 하드코딩 금지,
  기준은 항상 주입받는다. (기준 관리 방식 자체는 미정 — Wiki형 가능성 있음)
- GPU·모델 배치는 배포 설정(docker-compose + config)으로만 결정 — 코드에 GPU 번호·서버 주소·워커 수
  하드코딩 금지. llm_client는 vLLM 엔드포인트 호출 단일 창구, OCR은 doc_parser가 설정된 디바이스에서 실행,
  GPU 동시성·경합은 작업 큐가 관리. 마이그레이션 = 설정 교체로 끝나야 한다.
- 모든 검사 Agent는 공통 Finding 스키마로 반환:
  `check_id · agent · severity(major|minor|info) · kind(선택) · location(doc_id·페이지·장절·좌표) · finding · evidence · suggestion(선택) · status(미반영|반영됨|해당없음)`
  — severity 는 **중대성** 3단이다(막는 것 · 고쳐야 하는 것 · 참고). "규칙이라 확실한가
  LLM 판단인가"는 여기 담지 않는다 — 지적의 성질이 아니라 출처의 문제고, 이미
  checker 에 기록된다. 검사 자체를 못 한 것은 `unreviewed` 가 진다.
  — kind 는 **어떤 종류의 잘못인가**다(표현 점검이면 모순·표기·모호). severity 가
  "얼마나 급한가"라면 이건 "무엇이 잘못됐나"다. 한 검사기가 성격이 다른 지적을 여럿
  낼 때만 채운다 — 안 채우면 화면이 검사기 이름으로 되돌아간다. 없을 때 심각도 색만
  남으면 검토자는 오타와 앞뒤 모순을 구별하지 못한다(2026-08-06 추가).
  — location의 doc_id는 문서 간 지적(Lv3 추적성) 대비. 생성 계열 Agent(agent_generate)는 예외로
  Finding이 아니라 산출물 파일 + 메타를 반환한다.
- doc_parser의 공통 문서 모델은 JSON 직렬화 가능해야 한다 — 저장·재검증·Lv3 문서 DB 대비.
- 각 모듈 README에 의존성 목록 명시 — 모듈만 떼어갈 때 설치할 패키지를 알 수 있게.

## 기술 스택
- 확정: Python + uv(pyproject.toml), FastAPI, 사내 vLLM(Qwen3), Docker.
- 프론트: `web/` 은 바닐라 JS — 빌드 도구 없이 `serve` 가 그대로 얹는다.
  React 이관은 **미정**. 필요해지면 화면 단위로 마운트해 부분 도입한다(전체 재작성 금지).
- PDF 파싱 확정: pdf-inspector(페이지 분류·텍스트 추출, 텍스트 페이지는 OCR 안 태움) → 스캔 페이지만 PaddleOCR
  → 구조 복원 Docling → 그림·다이어그램 의미 해석 필요 시에만 Qwen3-VL.
  8/1 게이트에 pdf-inspector 한글 추출 품질 검증 포함 — 실패 시 텍스트 추출만 PyMuPDF로 교체(라우팅 구조는 유지).
- 미정: DB·작업 큐·문서 저장소. 미정 영역의 라이브러리 임의 도입 금지, 추가는 팀 합의 후에만.

## 기능 방침 (확정)
- 계정: 자체 최소형 — id/pw + 소속 팀 선택, 권한은 일반/관리자 2등급.
- 문서 보관: 원본·리포트·검토이력 보관, 보관 기간 경과 시 자동 삭제 (기간은 추후 결정).
- 심각도를 가르는 기준은 **"이 지적이 남아 있으면 팀이 이 문서를 그대로 낼 수 있는가"** 하나다.
  못 낸다 → `major` · 고쳐야 하지만 낼 수는 있다 → `minor` · 문서 결함이 아니라 검토 과정
  보고(미검토·절단·필터링) → `info`. 새 체커를 쓸 때 이 한 문장으로 정한다.
  아직 팀이 정해준 값이 아니다 — 원본 요구사항 xlsx 에 등급 열이 없어 우리가 정했다.
  팀이 "우리는 다르다" 고 말하면 그쪽이 이긴다. 그때는 코드가 아니라 **기준이** 등급을
  지게 옮긴다(`Criterion` 에 자리를 만든다) — 규칙값을 엔진에 박지 않는다는 원칙 그대로다.
- 점수(100점 만점 같은 단일 숫자): **넣지 않는다.** 한 번 있었다가 뺐다 — 가중치도
  구간도 실측 근거가 없었고, 실제로는 "분량 + LLM 이 얼마나 말이 많았나"를 재고 있었다
  (긴 문서가 40점 불리했다). 다시 넣으려면 **정답 문서 셋으로 눈금을 검증한 뒤**다.
  그전까지 화면은 심각도별 건수만 말한다.
- 품질: 팀별 정답 문서 셋(문서 + 기대 Finding) 기반 자동 재검증을 처음부터 구축 —
  검사 로직 수정 시 재검증 통과 후 커밋.

## 개발 원칙
- ponytail 필수 — `.claude/settings.json`이 설치를 유도한다. 미설치 상태로 작업 금지.
- 최소 구현(YAGNI): stdlib → 기존 의존성 → 최소 코드 순. 구현체 1개짜리 추상화·미래 대비 스캐폴딩 금지.
- 단, 입력 검증·에러 처리·보안은 생략 금지. 의도적 단순화는 `# ponytail:` 주석으로 남긴다.
- 공개 함수 타입힌트 필수, 포맷·린트는 ruff.
- 비트리비얼 로직엔 실행 가능한 체크 최소 1개(test_*.py). 커밋 전 통과 확인.

## Git
- `GIT_CONVENTIONS.md` 준수: 브랜치 `feat|fix|docs/<주제>`, 커밋 `타입: 한국어 요약`, main 직접 커밋 금지.

## 금지
- `.env`·인증키·사내 문서 원본·50MB 이상 파일·Claude 로그 커밋 금지.
  (예외: `golden/`의 연습용 가공 문서 — 실무 원본은 예외 없이 금지)
- 사내 문서를 외부 서비스(외부 LLM API 포함)로 전송 금지.
