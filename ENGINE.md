# DocSuree 검토 엔진

제품 소개와 빠른 시작은 [README](README.md) 에 있다. 이 문서는 **엔진이 실제로
어떻게 도는가** — 기준이 어떻게 검사가 되고, 무엇이 자동이고, 무엇을 못 하는가다.

---

## 파싱과 뷰잉은 별개다

**검토(파싱)는 포맷마다 네이티브로 읽는다** — LibreOffice가 필요 없다.

`pdfplumber` 를 core 의존성으로 올린 이유: `pypdf` 의 `extract_text()` 는 셀 안에
줄바꿈이 있는 표에서 한 행을 3~5줄로 파열시킨다. 이 문서들은 **표 안 글자가
54~88%** 라 그대로 두면 표 검사가 통째로 무너진다. 대신 책갈피(outline) API 는
`pdfplumber` 에 없어서 `pypdf` 를 남겼다.

그림은 본문에 `[그림 N: 설명]` 으로 들어간다. 설명은 비전 모델이 채우고(아래 `LLM
설정` 참고), 그 설명에서 나온 지적은 **그림 자체에** 형광펜이 얹힌다 — 설명은 파싱
본문에만 있고 PDF 텍스트 레이어에는 없어서 인용문으로는 위치를 찾을 수 없다.
짝짓기는 원본 크기로 한다(`report.match_images`); 짝을 못 지으면 짚지 않는다.

**뷰어용 PDF만 LibreOffice를 쓴다.** 화면에서 원본 레이아웃 위에 형광펜을 얹기
위한 것이라, 없어도 검토 자체는 돌아간다.

<details>
<summary><b>hwp 파싱 (rhwp) — 설치 후 한 번 고쳐야 한다</b></summary>

`uv sync` 로 같이 깔리지만, 0.8.1 linux 휠이 낡은 freetype 을 번들해
`import rhwp` 가 `undefined symbol: FT_Palette_Data_Get` 로 깨진다. 한 번 고쳐 둔다:

```bash
./scripts/fix-rhwp-freetype.sh
```

상류가 휠을 고치면 스크립트째 지운다.

</details>

<details>
<summary><b>뷰어용 PDF 변환 (선택 의존성)</b></summary>

hwp·hwpx·docx를 PDF 뷰어로 보려면 **LibreOffice + H2Orestart 확장 + Java**가 필요하다.

```bash
sudo apt install libreoffice default-jre
# H2Orestart.oxt 를 https://github.com/ebandal/H2Orestart/releases 에서 받아
sudo unopkg add --shared H2Orestart.oxt   # --shared 필수
```

`--shared`가 핵심이다. 서버는 변환마다 격리된 LibreOffice 프로필을 쓰므로, 사용자
프로필(`--shared` 없이)에만 설치하면 변환이 "source file could not be loaded"로 실패한다.

미설치 시: **hwpx**는 추출 텍스트 재현본으로 폴백(원본 레이아웃 아님), **hwp**는
변환 실패 안내가 뜬다. 둘 다 **검토 결과에는 영향이 없다** — 화면만 달라진다.

</details>

---

## 검토 엔진

### 한눈에 — 기준이 검토를 이끈다

```
  presets/criteria/*.yaml          ← 검토 "기준"(Criterion). 사람 말로 쓴 지시문
                                     팀 요구사항 엑셀에서 옮겨 온 뒤 손으로 관리한다
        │
        │  ┌─ 공통  common.yaml        모든 팀
        ├──┼─ 팀별  teams/<팀>.yaml    로그인한 팀
        │  └─ 업로드 사용자가 올린 것
        │        resolve_criteria() 가 셋을 합친다 (공통 → 팀 → 업로드)
        ▼
  기준마다 mode 가 갈림길을 정한다
        │
   ┌────┴─────────────┬──────────────────┬───────────────┐
   ▼                  ▼                  ▼               ▼
 mode: 규칙       mode: LLM-조각     mode: LLM-문서    mode: 사람
   │                  │                  │               │
 check: 이름       청크마다 물음      통째로 한 번      검사기 없음
   │                  │                  │               │
 체커 코드          ChunkCriteriaChecker  WholeDocCriteriaChecker   검토자가 직접
 (LLM 안 씀)        ─── 사내 vLLM (Qwen3) ───            체크박스로 판정
   │                  │                  │               │
   └──────────────────┴──────────────────┴───────────────┘
                       ▼
              Finding 목록  (지적 · 미검토 · 통과)
                       ▼
              항목별 결과 → 리포트 · 화면 하이라이트
```

**문서 쪽 흐름**은 그 아래에서 따로 돈다:

```
문서 → 적재(ingestion) → 정규화(normalize) → 청킹(chunking) → 체커 실행 → 수집(collect)
```

`orchestrator.review_document_by_criteria()` 가 둘을 맞물린다. 업로드 기준을
고르지 않은 일반 단일 검토도 이 경로를 쓴다. 체커는 모두 같은 모양이라
(`check(doc, ctx) -> list[Finding]`) 추가·제거해도 리포트·웹·CLI를 고칠 일이 없다.

### 기준 한 줄이 어떻게 검사가 되나

```yaml
# presets/criteria/teams/EV3.yaml
- 'no': '13'
  text: |-
    제출문서 파일명을 아래 양식으로 작성되었는가를 확인
    예) Z11008-940VD-011C_Rev.08_RVVR for ESF-CCS for SKN56
    계통문서번호_버전_문서명
  agent: 형식·완전성     # ① 어느 관점인가 (화면 묶음 · mode 기본값)
  mode: 규칙             # ② 어떻게 물을 것인가 (LLM 안 씀)
  check: filename        # ③ 무엇이 검사하나 (FilenameChecker)
  params:                # ④ 그 검사에 줄 값 (코드에 안 박는다)
    filename_pattern: '[^_]+_Rev\.\d+_.+'
```

세 축이 하는 일이 다르다:

| 축 | 값 | 정하는 것 |
|---|---|---|
| `agent` | 관점 6종 | 어느 관점인가 · `mode` 기본값 · 화면 묶음 |
| `mode` | `규칙` `LLM-조각` `LLM-문서` `사람` | **어떻게** 물을 것인가 |
| `check` | 체커 이름 6종 | `규칙` 일 때 **무엇이** 검사하나 |

**`check` 가 없으면 "사람이 확인" 으로 떨어진다.** `mode: 규칙` 이라고 적혀 있어도
그 기준을 볼 체커가 없으면 자동이라고 말하지 않는다.

> 예전에는 `agent` 라벨로 체커를 이었다. 라벨은 여섯 개인데 규칙 기준은 EV2 만 16개라,
> 체커 둘이 낸 지적이 16개 **전부에 복사**됐다. TBD 하나가 열여섯 건으로 보이고,
> 실제로 검사되지 않은 열넷이 "검사됨"으로 표시됐다. `check` 가 그 자리를 가져갔다.

### 지금 몇 개가 진짜 자동인가

팀 기준 총 125건(공통 ∪ 팀별) 기준:

| mode | 건수 | LLM |
|---|---:|---|
| `규칙` | 36 | ❌ 안 씀 — 체커 코드가 검사 |
| `LLM-조각` | 27 | ✅ 청크마다 물음 |
| `LLM-문서` | 28 | ✅ 통째로 한 번 물음 |
| `사람` | 34 | — 검사기 없음 |

`규칙` 36건 중 실제로 체커가 붙은 것은 아직 **일부**다. 나머지는 화면에서
"이 기준을 검사하는 규칙이 아직 없습니다" 로 뜬다 — 못 하는 것을 못 한다고 말하는
것이 이 도구의 기본 자세다(아래 "미검토는 통과가 아니다").

**PDF는 책갈피(outline)에서 섹션 트리를 복원한다.** PDF에는 마크다운 heading이
없어서 예전에는 쪽마다 섹션을 하나씩 만들었고, 제목이 `1쪽`·`2쪽`이 되었다.
그러면 `required_sections`를 무엇으로 채우든 매칭되지 않아 **채우는 순간 100%
오탐**이 되고, 지적 위치도 "3쪽"까지만 나온다. 실문서에는 책갈피가 있어서
(실측: SHN34 SRS 380개 · CDMS RVVR 67개) 그걸 heading으로 심는다.

책갈피가 없는 PDF는 예전처럼 쪽 단위로 떨어진다 — 없는 구조를 지어내지 않는다.
실제로 그런 문서가 있다(SKN56 CPS SRS 0개, 내부검토 체크리스트 2번 항목 위반).

### 체커

**규칙 체커** — LLM 없이 돈다. 기준이 `check:` 로 이름을 대면 붙는다.

| `check:` | 체커 | 무엇을 보는가 | 기준이 주는 값 |
|---|---|---|---|
| `required_sections` | `CompletenessChecker` | 필수 장절이 있나. 번호로 절을 찾고 제목은 따로 대조 | `required_sections` |
| `placeholder` | `PlaceholderChecker` | 남은 자리표시자. 기본 `["TBD"]` | `placeholder_markers` |
| `field_presence` | `FieldPresenceChecker` | 표·결재란의 칸 값(빈 칸·형식·날짜·체크박스) | `fields` |
| `filename` | `FilenameChecker` | 제출 파일명이 팀 규칙(정규식)에 맞나 | `filename_pattern` |
| `abbrev` | `AbbrevChecker` | 약어 목록 ↔ 본문 **양방향** (미등록 · 미사용) | `abbrev_sections` `abbrev_ignore` |
| `reflist` | `RefListChecker` | 참조문서 목록 ↔ 본문 인용 **양방향**. 절 번호로 맞춘다 | `ref_sections` |

양방향 체커 둘(`abbrev` · `reflist`)은 **여러 건을 한 지적으로 묶는다.** 낱개로 내면
실문서 하나에서 174건이 쏟아져(실측: SHN34 RVVR 약어) 다른 검사의 지적까지 파묻는다.
기준 원문도 낱개가 아니라 "식별하고 **리스트화**"를 요구한다. 토큰마다의 위치는
`evidence` 로 남아 화면이 짚어 갈 수 있다.

**LLM 체커** — `mode` 가 정한다. 기준 텍스트가 그대로 프롬프트에 실리므로 `check:` 가 없다.

| mode | 체커 | 방식 |
|---|---|---|
| `LLM-조각` | `ChunkCriteriaChecker` | 조각 기준 **전부를 한 벌에 묶어** 청크마다 1회 왕복 |
| `LLM-문서` | `WholeDocCriteriaChecker` | 문서 기준을 묶어 통째로 한 번. 창을 넘으면 조각으로 내려가고 **그 사실을 밝힌다** |

체커마다 기준 하나가 아니라, **`check:` 이름 하나에 체커 하나**다. 같은 이름을 댄
기준 둘은 같은 지적을 받는다(데이터가 같은 검사를 두 번 적은 것). 단 `params` 를 가진
기준은 따로 만든다 — 표지 항목과 개정기록 항목이 한 벌을 나눠 가지면 지적이 똑같아진다.

**2문서 비교(`compare`)** 는 별도로 `TraceabilityChecker`(요건 ID 양방향 대조)와
`ContentMatchChecker`(내용 일치, LLM)를 돌린다. 추적성 판정은 다섯 가지다 —
`linked` · `missing` · `orphan` · `out_of_scope`(하위문서 담당 밖) ·
`rolled_up`(아래 참고). 뒤의 둘은 지적이 아니지만 RTM에 남아 개수가 보인다.

### 미검토는 통과가 아니다

이 도구가 낼 수 있는 최악의 거짓말은 **"검사 안 했는데 이상 없다고 말하는 것"** 이다.
그래서 항목 상태가 넷이다:

| 상태 | 뜻 |
|---|---|
| `flagged` | 검사했고 지적이 나왔다 |
| `clean` | 검사했고 이상 없다 |
| `unreviewed` | **못 했다** — 잴 자가 없거나 문서에서 못 찾았다 |
| `manual` | 검사기가 없다. 사람 몫 |

체커는 검사할 것을 하나도 못 받으면 빈 목록이 아니라 `unreviewed` Finding 을 낸다:

- 필수 절 목록이 기준에 없음 → "필수 절 목록이 검토 기준에 없어 수행하지 않았습니다"
- 요건 ID 형식이 없음 → 문서 비교가 "추적성 검사를 수행하지 않았습니다"
- 약어 장절을 못 찾음 · 볼 칸이 없음 · 파일명 규칙이 없음 → 각각 같은 모양
- 기준이 하나도 안 들어옴 → "적용할 검토 기준이 없어 이 문서를 검사하지 않았습니다"

### 기준은 세 층이다

층은 **파일 위치**가 정한다. 파일 안에 `scope:` 를 적어도 안 읽는다 — `teams/` 에 둔
파일이 "공통"이라고 우기면 그건 실수이고, 그 실수가 조용히 통하면 공통 기준이 한
팀에서만 도는 것을 아무도 눈치채지 못한다.

| 층 | 자리 | 적용 |
|---|---|---|
| 공통 | `presets/criteria/common.yaml` | 모든 팀·모든 문서 |
| 팀별 | `presets/criteria/teams/<팀>.yaml` | 로그인한 팀 |
| 업로드 | `.docreview/criteria/uploads/*.json` | 그 검토에만 |

`resolve_criteria()` 가 **공통 → 팀 → 업로드** 순으로 합치고 `text` 로 중복을 거른다.
번호(`no`)는 파일 안에서만 유일하므로, 층이 겹치면 뒤 층에 꼬리표를 단다
(`16` → `16(팀별)`) — 안 그러면 공통 16번의 판정이 팀 16번에 얹힌다.

**검사 매개변수도 기준이 정한다.** 코드에도 설정 파일에도 안 박는다:

```yaml
  params:
    required_sections: ["1.0 Purpose", "2.0 Scope", ...]   # EV2 40번
    id_pattern: "FR-[A-Z]{2,4}(?:_\\d+)+"                  # 아직 아무 팀도 안 적었다
    id_rollup_separator: "_"     # 하위요건(FR-GC_01_01)을 부모로 한 단계 접는다
    scope_pattern: ""            # 부분 담당이면 그 범위
```

> 예전에는 `presets/checklists/*.yaml` 이 이 값들을 갖고 있었다. **특정 문서를 보고
> 거꾸로 뽑은 값이라 순환이었다** — 문서에서 뽑은 잣대로 그 문서를 재면 틀릴 수가 없다.
> 지웠고, 지금은 **적혀 있지 않으면 그대로 둔다.** 그래서 `id_pattern` 이 비어 있고,
> 문서 비교는 돌지 않으면서 "요건 ID 형식이 검토 기준에 없어 수행하지 않았습니다" 로 뜬다.
> 지어낸 잣대보다 못 했다는 말이 낫다.

`id_rollup_separator` 는 오탐 제어에 결정적이다. 상위문서가 요건을
`FR-CCG_01_01`처럼 더 잘게 쪼개 쓰고 하위문서는 부모 수준에서만 검증하는
문서쌍이 있다. 접지 않으면 **누락 54건 중 46건이 오탐**이 되는데, 실측 결과
그 46건 **전부** 부모 ID가 하위문서에 있었다(SHN34 SRS↔RVVR).

`scope_pattern` 도 같은 성격이다 — 부분 설계서에서 지정하지 않으면 남의 몫까지
"누락"으로 세어 소음이 된다.

**같은 실에서 온 문서라도 ID 체계는 문서군마다 다르다.** SHN34는 `FR-GC_01`,
SKN56은 `FR1-0305`라 서로의 잣대로는 **한 개도 안 걸린다.** 그런데 그 실패는
오류가 아니라 조용한 "0건"이다 → 그래서 자동 감지를 둔다(아래).

한 문서 안에서 동일 ID의 명칭·값·조건이 흔들리는지는 해당 팀의 `LLM-문서`
기준으로 검사한다. 예를 들어 EV2 32번은 본문·추적성 표의 ID를 대조하고,
동일 ID가 반복되면 모든 등장 위치의 서술을 비교하도록 명시한다.

### 기준 자동 감지

업로드한 문서를 **팀 기준마다 재서** 요건 ID가 몇 개 걸리는지 세어 준다.

```
POST /api/detect   → {"detected": [{id, id_example, matches, has_pattern, …}], "best": …}
```

화면은 검토자가 기준을 직접 고르지 않았으면 감지 결과로 맞추고, 직접 골랐으면
덮지 않되 **매칭이 0개면 경고**한다. 아무것도 안 맞으면 `best`는 `null`이다 —
지어낸 추천은 이 기능이 막으려던 사고를 다시 만든다.

> 지금은 어느 팀 기준도 `params.id_pattern` 을 적지 않아 `has_pattern` 이 전부
> `false` 다. 감지가 "아무것도 안 맞음"이라고 답하는 것은 문서가 이상해서가 아니라
> **잴 자가 없어서**다. 팀이 ID 형식을 알려주면 그 기준에 한 줄 넣으면 된다.

동일 ID 서술 일관성 기준은 필요한 팀 preset에 둔다(EV2 32번). 불일치의 정의는
오탐을 막기 위해 **의도적으로 좁힌다**:

- 불일치 **O** — 같은 대상을 다른 이름으로 / 같은 항목에 다른 값·조건 / 명백한 오탈자
- 불일치 **X** — 정상적인 하위 기능 구조 / 한쪽이 더 자세하거나 생략한 것 / 서로 다른 대상

`"어긋나거나 빠뜨렸으면"` 처럼 열어 두었더니 모델이 "덜 상세하다"까지 불일치로
올려 **precision이 57%까지 떨어졌다.** 그래서 preset은 정상적인 상세 설명 추가나
하위 기능 설명을 불일치로 보지 말라고 함께 명시한다.

---

## LLM 설정

창구는 **둘**이다. 문서 검사용 텍스트 모델과 그림 해석용 비전 모델이 서로 다른
엔드포인트다 — 서버가 GPU 배치까지 달리해 따로 띄우므로, 주소를 하나로 합쳐 두면
한쪽을 옮길 때 다른 쪽이 조용히 끊긴다.

| 창구 | 환경변수 | 쓰는 곳 | 없으면 |
|---|---|---|---|
| 문서 검사 | `LLM_QWEN_URL` | 표현 점검 · 일관성 대조 · 내용 일치 | 해당 항목이 **"검사 안 됨"** |
| 그림 해석 | `LLM_OCR_URL` | 그림 안의 표·구성도를 읽어 본문에 넣는다 | 그림을 읽지 않았다고 보고 |

모델명은 서버가 `--served-model-name` 으로 고정한 별칭(`qwen`·`ocr`)을 쓴다. 주소를
환경변수로 받으면 설정 파일의 모델명은 **다른 서버 것**이므로 무시한다 — 실제로
그러다 HTTP 404 로 83개 청크가 전부 실패했다. 별칭이 다르면 `LLM_QWEN_MODEL` 로 준다.

**화면에 뜨는 이름은 별칭과 별개다.** 사람에게 `qwen` 은 뜻이 없어서, 설정의
`model_label`(또는 `LLM_QWEN_LABEL`)을 화면에 쓴다. 호출에는 절대 쓰지 않는다.
서버 모델을 바꾸면 이 값도 같이 고칠 것 — 자동으로 알 방법이 없다(vLLM `/v1/models`
도 별칭만 답한다).

`GET /api/health` 가 `llm_label` · `vlm_enabled` 를 내려주고, 화면은 그것으로 칩을
그린다: `[ 규칙만 · 빠름 ]  [ Qwen3.6-27B · 사내 · 그림 포함 ]`. 그림 해석이 꺼져
있으면 `· 그림 포함` 이 없고, 검토 결과에 "그림 N장을 읽지 않았습니다"가 올라온다.

### 주소는 환경변수가 이긴다

```bash
export LLM_QWEN_URL=http://<서버>:9001/v1
export LLM_OCR_URL=http://<서버>:9002/v1      # 선택
uv run docsuree serve
```

설정 파일을 고칠 필요가 없다 — 주소를 주면 그 서버로 붙는다. 서버 주소는 배포마다
다르고 배포판 저장소에 사내 주소를 담을 수 없어서, 코드도 설정 파일도 아니라
**배포 환경이 정한다.**

> LLM 서버와 DocSuree 가 같은 기계면 `127.0.0.1` 로 충분하다. 다만 나중에
> docker-compose 로 감싸면 컨테이너 안의 `127.0.0.1` 은 호스트가 아니라 컨테이너
> 자신이다 — 그때는 `network_mode: host` 나 호스트 IP 가 필요하다.

연동 확인 (주소를 안 주면 스스로 skip 한다):

```bash
LLM_QWEN_URL=... LLM_OCR_URL=... uv run pytest src/modules/llm_client -v
```

### 설정 파일

```toml
[llm]
provider  = "local"   # OpenAI 호환 서버에 붙는다는 뜻. 아래 base_url 이 그 서버다
                      # ("이 기계"가 아니다 — 사내 vLLM 을 가리킨다)
model     = "qwen"    # 서버가 고정한 별칭. 전체 이름을 보내면 서버가 모른다
base_url  = ""        # LLM_QWEN_URL 로 준다 (아래 참조)
thinking  = false     # 사고 ON 76.8초 → OFF 1.1초. 정확도는 그대로였다
max_tokens = 1024
vlm_base_url = ""     # LLM_OCR_URL 로 준다. 비면 그림 해석 안 함
vlm_model = "ocr"
```

**설정 파일은 `settings.toml` 하나다.** 주소는 거기 적지 않는다 — 배포마다 다르고
저장소에 담을 것이 아니다. 문서 검사(`:9001`)와 그림 해석(`:9002`) 두 창구를 다 쓰고,
둘 다 환경변수로 준다:

```bash
LLM_QWEN_URL=http://다른서버:9001/v1 uv run docsuree serve
```

> **주소를 localhost 로 두지 않는 이유.** GPU 기계에서 작업하는 사람에게는
> `127.0.0.1` 이 닿지만 나머지 사람에게는 안 닿는다. 기본값은 모두에게 동작해야
> 하므로 LAN 주소를 쓴다 — GPU 기계에서도 자기 주소로 그대로 닿는다.

- 화면의 **"규칙만 · 빠름"** 은 두 창구를 **모두** 끈다(`_rules_only`). 텍스트 모델만
  끄면 "빠름"을 골랐는데도 그림 해석이 돌아 느려진다.

### 속도 대가

요구사항명세서 기준 실측:

| 설정 | 시간 | 지적 |
|---|---|---|
| `echo` (규칙만) | 0.17초 | 6건 (전부 규칙) |
| `local` Qwen3.6-27B | 73.6초 | 64건 (규칙 6 + LLM 58) |

느린 만큼 값어치는 있었다. LLM이 잡은 것 중에는 규칙으로는 불가능한 진짜 결함이
있었다 — 식별자 설명("분류구분")과 실제 형식("세부기술번호")의 불일치,
"해안선 변화 예측"(11회) ↔ "해안선 변화예측"(23회) 용어 흔들림.

빠르게만 보려면 웹 UI에서 **"규칙만 · 빠름"** 을 고른다. 런타임에 provider를
`echo` 로 바꿔 LLM을 아예 부르지 않는다.

### LLM 지적의 지위

LLM이 낸 지적은 **전부 MINOR** 이고 `"(LLM 제안, 검증 필요)"` 꼬리표가 붙는다.
규칙기반 지적(MAJOR)보다 낮게 두어 정렬에서 뒤로 간다. LLM 판정을 확정이 아니라
**사람이 확인할 것(triage)** 으로 취급하는 설계다.

---

## 검토 이력

검토·비교 결과는 `.docreview/history/` 에 JSON으로 남는다(최근 200건). 홈의 "최근
검토"나 "기록" 화면에서 클릭하면 문서를 다시 올리지 않고 그때 그 결과가 열린다.

업로드본은 임시 디렉터리에서 분석하고 바로 지운다. 다만 **단일 검토는 원본을
최근 20건까지 함께 보관한다**(`history.MAX_ORIGINALS`) — 이력에서 결과를 다시
열 때 뷰어로 원본을 보여주기 위해서다. 오래된 것부터 밀려난다.

> ⚠️ 고객 문서가 서버에 쌓이는 지점이므로 알고 있어야 한다. 보관하지 않으려면
> `MAX_ORIGINALS = 0` 으로 두면 된다(결과는 그대로 남고 뷰어만 텍스트 폴백).
> 2문서 비교는 원본을 보관하지 않는다.

```
GET    /api/history                 최근 목록
GET    /api/history/{id}            저장된 결과 전체
GET    /api/history/{id}/original   보관된 원본 (없으면 404 → 텍스트 폴백)
DELETE /api/history/{id}            삭제
```

### 그 밖의 API

```
POST /api/review      단일 검토 (SSE로 단계 진행을 흘린다)
POST /api/compare     2문서 비교
POST /api/detect      체크리스트 자동 감지
POST /api/annotate    원본 PDF에 지적을 형광펜으로 표시 + 요약 페이지
POST /api/suggest     지적 하나에 대한 수정안 (검토자가 눌렀을 때만)
POST /api/render-pdf  비-PDF를 PDF로 변환 (뷰어가 PDF만 다루게)
GET  /api/health      지금 적용 중인 기준·모델
```
---

## 알려진 한계

- **규칙 기준 36건 중 체커가 붙은 것은 일부** — 나머지는 화면에서 "이 기준을 검사하는
  규칙이 아직 없습니다" 로 뜬다. 없는 것을 없다고 말하므로 거짓말은 아니지만, 팀이
  기대하는 자동화 폭에는 못 미친다. 체커를 하나 만들 때마다 yaml 에 `check:` 한 줄이다.
- **`id_pattern` 을 어느 팀도 안 적었다** — 그래서 문서 비교(추적성)가 돌지 않는다.
  옛 `presets/checklists/` 의 값은 문서에서 거꾸로 뽑은 순환이라 지웠다. 팀이 ID 형식을
  알려주기 전까지는 "검사 못 함"으로 남는다.
- **`required_sections` 는 EV2 만 적었다** — 나머지 여섯 팀은 공통 9번이 "필수 절 목록이
  검토 기준에 없습니다"로 뜬다. 추측으로 채우면 없는 결함을 만들어내므로 비워 둔다.
- **약어 체커의 미등록 방향에 도메인 명사가 섞인다** — 실문서 넷으로 재서 907 → 423
  까지 줄였고(숫자 섞인 토큰 제외 · 대문자 줄 제외), 남은 것은 `ANALOG` `CABINET`
  `DAMPER` 처럼 대문자로 쓴 도메인 명사다. 영어 기능어 목록을 넣어 봤지만 174 → 170
  이라 **안 넣었다** — 남는 것은 팀별 `params.abbrev_ignore` 몫이다. 한글 전문용어는
  형태로 못 가려서 아예 안 잡는다.
  - 다만 실측에서 미등록의 상당수가 **진짜 지적**이었다. SHN34 RVVR 은 약어 30여 개를
    등록해 두고 `AFAS`(856회) `MCR`(810회) `SIAS`(438회) 를 목록에 안 올렸다.
- **`golden/` 이 아직 없다** — 위 숫자는 `data/` 의 실문서로 잰 것이라 "안 터졌다"
  까지만 안다. 검사 로직을 조였을 때 **잡던 것을 놓쳤는지**는 정답 셋이 있어야 안다
  (CLAUDE.md: 검사 로직 수정 시 재검증 통과 후 커밋).
- **심각도의 축이 체커마다 다르다** — major/minor 를 가르는 기준이 두 갈래로
  갈려 있다. 규칙 체커는 **"얼마나 나쁜가"** 로 나눈다(`reflist`: 인용했는데
  목록에 없음 major / 목록에 있는데 인용 안 함 minor. `completeness`: 절 누락
  major / 제목만 다름 minor). LLM 체커는 **"얼마나 확실한가"** 로 나눈다 —
  판단이라 사람 확인이 필요하니 minor 다. 그래서 지금 *LLM 이 찾은 요구사항
  모순*과 *파일명에 남은 금지어*가 같은 minor 다. 한 축으로 정해야 한다.
- **minor 가 전체의 93%** (실측: 단일 검토 176건 · 지적 2,248건 중 minor 2,091 ·
  info 96 · major 61). 등급이 있어도 사실상 한 덩어리라 "무엇부터 볼 것인가"를
  거의 못 가른다.
- **`consistency` 한 체커가 지적의 93%를 낸다** (2,088/2,248 · 문서당 평균 12건).
  등급을 어떻게 다시 나누든 이 체커가 들어가는 칸이 곧 전부가 되므로, 등급 재배치
  전에 **이만큼 낼 만한가**를 먼저 봐야 한다.
